============
Installation
============


Build package
=============

At the command line::

    $ conda create -n ccfb
    $ source activate ccfb
    $ make install


Installation
============

At the command line::

    $ pip install ccfb-api

